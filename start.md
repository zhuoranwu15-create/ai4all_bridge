项目启动文档 (PID)：微信个人 AI 陪伴与生活助理项目 (Phase 1)
1. 项目背景与动机 (Motivation)
当前，优秀的开源大模型网关（以 OpenClaw 为代表）为极客群体提供了强大的个人 AI 助理体验。然而，对于不具备技术背景的普通用户而言，部署配置的门槛极高；同时，原生架构的“单机1:1”属性，也限制了更多普通用户低成本获得类似体验。

本项目旨在构建一个“中心化智能、边缘端免配”的个人 AI 陪伴与生活助理服务。
豆包等通用 AI 产品已经很好地满足了大量工作效率场景。本项目的第一阶段不以办公提效为核心，而是更聚焦于情感陪伴、日常闲聊、生活建议、语音互动和长期记忆等更贴近日常关系感的体验。我们将利用微信作为零学习成本的国民级入口，让用户无需部署、无需配置，只需添加特定微信号，就可以低成本使用类 OpenClaw 架构的个人 AI 陪伴与助理能力。

在技术上，我们将彻底解耦“通信 I/O 通道”与“AI 业务逻辑”，将 OpenClaw 降维为通信基建，而在自定义的后端中重构一套支持多用户、会话隔离、人格配置、记忆管理和成本控制的业务引擎。第一阶段重点交付一个稳定可用的个人 AI 陪伴与生活助理服务，并为未来更多应用层业务预留清晰的架构边界。

2. 核心产品需求与特性 (Core Product Features)
本阶段（Phase 1）的产品定位是：稳定、免配置、有记忆、有陪伴感的个人 AI 文本与语音对话服务。

对 C 端用户的核心交付：

零配置接入： 无需安装任何软件，添加特定微信号即可开始对话。

陪伴式交互： 拥有独立的人设（Soul）与稳定的表达风格，避免冷冰冰的工具感，优先服务情感陪伴、日常闲聊与生活助理类场景。

连贯性记忆： 能够记住上下文，支持多轮深度对话。

文本与语音优先： Phase 1 优先保证私聊文本对话链路稳定，并支持基础语音输入处理，覆盖用户在日常陪伴和生活助理场景中的自然表达习惯；图片、群聊等多模态或复杂场景作为后续扩展项，不进入首版必做范围。

对 B 端（管理者）的核心交付：

资源池化复用： 多个用户共享后端的 LLM 与 ASR API 额度，降低个人使用门槛和单用户成本。

会话逻辑隔离： 以用户与会话为基本边界隔离上下文、历史记忆与用量统计，避免用户之间的信息串线。Phase 1 目标是可靠的逻辑隔离；更高等级的租户级数据库、密钥、日志或向量库隔离可在后续版本演进。

成本与流量可控： 具备基础的请求频次限制与 Token 消耗阻断机制。

3. 总体技术架构 (System Architecture)
系统采用 “三层解耦架构”，确保高可用性与故障隔离。

接入网关层 (I/O & Protocol Layer)：

核心组件： 微信端 + OpenClaw（配置为纯转发模式）。

职责： 承担所有最脏最累的“协议级”工作。维持微信心跳、破解复杂网络环境、收取消息并提取关键元数据（如发送者 ID、群聊/私聊标识、媒体文件链接），然后将其作为标准 Webhook 事件推给中台。

业务控制中台 (Business Logic & Routing Hub)：

核心组件： 自研中控路由服务。

职责： 整个系统的“大脑皮层”。负责身份鉴权、记忆检索、人设注入、Prompt 编排、风控延迟控制，以及与大模型的 API 交互。

计算引擎层 (AI Engine Layer)：

核心组件： 商业大模型 API 或本地部署的 LLM。

职责： 提供纯粹的推理与生成能力。

4. 核心模块设计 (借用并超越 OpenClaw 的优秀理念)
为了保留 OpenClaw 原生带来的惊艳体验，我们将它的核心编排逻辑进行“后端化重构”。

4.1 动态灵魂引擎 (Dynamic Soul Engine)
借鉴点： 继承 OpenClaw Soul.md 的易用性，将 Prompt 配置代码化、文本化。

系统升级： 抛弃静态绑定。中控系统维护一个可配置的“人格库”。当收到消息时，根据用户的注册状态、默认配置或特定指令，动态从库中拉取对应的 System Prompt 文本流进行注入。这为日后的多业务场景或多角色能力扩展预留空间。

4.2 滑动窗口记忆池 (Sliding Window Memory Manager)
借鉴点： 借鉴其克制的历史记录回溯，既能保持上下文，又防止 Token 爆炸。

系统升级： 建立基于唯一标识（wxid）的主键数据库。每次对话前，引擎只拉取该用户的最近 N 轮对话，并结合 Token 计算探针进行硬截断。彻底解决原生架构并发时可能出现的内存指针错乱问题。

4.3 提示词组装流水线 (Prompt Assembler Pipeline)
借鉴点： 严密的数组组装逻辑。

系统升级： 建立一个标准化的流转 Pipeline：[动态人设] + [裁剪后的专属历史记忆] + [当前用户输入]。确保大模型在每一次无状态的 HTTP 请求中，都能恢复当前用户与当前会话的上下文。Phase 1 优先保证完整回复的可靠投递；流式输出（Streaming）和打字机体验作为后续增强能力预留接口。

5. 关键项目考量与高阶风险控制 (Key Considerations)
在代码落地前，必须在架构层面预留对以下风险的防范：

T0 级风险：账号稳定性与平台合规

微信对高频、异常、自动化特征明显的行为较为敏感。中台需要在核心模块中设计发送节流、排队、退避与异常熔断机制，避免秒级高频回复、重复发送、批量触达等不稳定行为。Phase 1 的目标是提升账号运行稳定性，并尽量符合平台使用边界。

T1 级风险：恶意消耗与 Token 刺客

面向多用户开放的服务容易被恶意消耗或异常高频使用。架构中必须包含 Rate Limiter（速率限制器）。基于 wxid 设定每日交互次数上限、单次对话最大上下文深度。

T2 级风险：指令注入防御

需在 System Prompt 层面增加护栏（Guardrails），防止用户通过类似于“忽略之前所有指令”的魔法语句，套取系统的架构信息或底层 Prompt。

演进扩展性 (Scalability for Phase 2)

本阶段的业务服务设计应尽量保持无状态，核心状态沉淀到数据库或缓存中，并通过清晰的模块边界解耦。Phase 1 需要接入基础 ASR（语音识别模块）以支持语音输入；同时为之后挂载 RAG（知识库检索模块）或其他应用层业务能力预留标准化接口。
